import ListBox from "./ListBox";
import WatchBox from "./WatchBox";


export default function Main({children}) {
    return (
        <main className="main">
            {children}
        </main>
    )
}